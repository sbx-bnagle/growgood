name = "growgood-github-oauth"
main = "workers/github-oauth.js"
compatibility_date = "2024-12-01"
account_id = "a0432d479ca11b124c5051f475d9b84f"  # Add your Cloudflare account ID here

# Secrets (set via: wrangler secret put GITHUB_CLIENT_ID)
# These are securely stored and injected at runtime

# Optional: Custom domain for this worker
# routes = [
#   { pattern = "auth.growgood.example.com", zone_name = "growgood.example.com" }
# ]
